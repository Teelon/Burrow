import type { DB } from '../client';
import type { INotificationRepository } from '../../../../infrastructure/types';
export declare function createNotificationRepository(db: DB): INotificationRepository;
