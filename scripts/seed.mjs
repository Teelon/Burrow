/** Demo data seeding (two users, projects, nested notepads, populated board).
 *  Lands with Phase 7 of PLAN.md; auth endpoints must exist first.
 */
console.log(
  'seed:local is not available yet — demo seed data arrives with Phase 7 (see PLAN.md, Phase 7 task 4).',
)
process.exit(0)
